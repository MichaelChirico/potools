# make sure we group by file name, not just node ID. create ~identical node structure on
#   this file and on copy2.R to make sure the node 'id' are the same
check <- function() {
  stop("copy two")
}

%s: window width longer than input vector, returning all NA vector\n
%s: align %d, shift answer by %d\n
%s: processing algo %u took %.3fs\n
%s: running for input length %<PRIu64>, window %d, hasna %d, narm %d\n
%s: hasNA=FALSE used but NA (or other non-finite) value(s) are present in input, use default hasNA=NA to avoid this warning
%s: NA (or other non-finite) value(s) are present in input, re-running with extra care for NAs\n
%s: hasNA=FALSE used but NA (or other non-finite) value(s) are present in input, use default hasNA=NA to avoid this warning
%s: NA (or other non-finite) value(s) are present in input, skip non-NA attempt and run with extra care for NAs\n
%s: running in parallel for input length %<PRIu64>, window %d, hasna %d, narm %d\n
%s: hasNA=FALSE used but NA (or other non-finite) value(s) are present in input, use default hasNA=NA to avoid this warning
%s: NA (or other non-finite) value(s) are present in input, re-running with extra care for NAs\n
%s: NA (or other non-finite) value(s) are present in input, na.rm was FALSE so in 'exact' implementation NAs were handled already, no need to re-run\n
%s: window width longer than input vector, returning all NA vector\n
%s: align %d, shift answer by %d\n
%s: processing algo %u took %.3fs\n
%s: running for input length %<PRIu64>, window %d, hasna %d, narm %d\n
%s: hasNA=FALSE used but NA (or other non-finite) value(s) are present in input, use default hasNA=NA to avoid this warning
%s: NA (or other non-finite) value(s) are present in input, re-running with extra care for NAs\n
%s: hasNA=FALSE used but NA (or other non-finite) value(s) are present in input, use default hasNA=NA to avoid this warning
%s: NA (or other non-finite) value(s) are present in input, skip non-NA attempt and run with extra care for NAs\n
%s: running in parallel for input length %<PRIu64>, window %d, hasna %d, narm %d\n
%s: hasNA=FALSE used but NA (or other non-finite) value(s) are present in input, use default hasNA=NA to avoid this warning
%s: NA (or other non-finite) value(s) are present in input, re-running with extra care for NAs\n
%s: NA (or other non-finite) value(s) are present in input, na.rm was FALSE so in 'exact' implementation NAs were handled already, no need to re-run\n
%s: window width longer than input vector, returning all NA vector\n
%s: results from provided FUN are not length 1
%s: results from provided FUN are not of type double, coercion from integer or logical will be applied on each iteration\n
%s: results from provided FUN are not of type double
%s: align %d, shift answer by %d\n
%s: took %.3fs\n

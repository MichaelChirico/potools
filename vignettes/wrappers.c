Attribute name must be a character vector of length 1
Internal structure doesn't seem to be a list. Can't set class to be 'data.table' or 'data.frame'. Use 'as.data.table()' or 'as.data.frame()' methods instead.
First argument to setlistelt must be a list()
Second argument to setlistelt must a length 1 integer vector
i (%d) is outside the range of items [1,%d]
%p
x isn't a VECSXP
dim.data.table expects a data.table as input (which is a list), but seems to be of type %s

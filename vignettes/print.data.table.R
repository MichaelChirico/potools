Valid options for col.names are 'auto', 'top', and 'none'
Valid options for trunc.cols are TRUE and FALSE
Column classes will be suppressed when col.names is 'none'
:

Internal structure doesn't seem to be a list. Possibly corrupt data.table.
Valid options for col.names are 'auto', 'top', and 'none'
Valid options for trunc.cols are TRUE and FALSE
Column classes will be suppressed when col.names is 'none'
:

Internal structure doesn't seem to be a list. Possibly corrupt data.table.
Index: %s\n
Indices: %s\n
%d variable not shown: %s\n
%d variables not shown: %s\n
